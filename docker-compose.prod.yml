version: '3.8'

services:
  postgres:
    # Production Security: Do not expose PostgreSQL port 5432 publicly on host
    ports: []
    restart: always

  backend:
    environment:
      SPRING_PROFILES_ACTIVE: prod
    restart: always
